/**
 * Shared max-width + horizontal padding so the header and dashboard pages align.
 * (Tailwind-only; keep in sync when changing layout.)
 */
export const dashboardHeaderShell =
  "mx-auto w-full min-w-0 max-w-6xl px-4 sm:px-6 lg:px-8" as const

export const dashboardPageShell =
  "mx-auto w-full min-w-0 max-w-6xl px-4 py-6 sm:px-6 sm:py-8 lg:px-8" as const
